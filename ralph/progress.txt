## 2026-02-18 — Feature 1.1: Initialize Next.js 15 project with Bun

Completed:
- Initialized Next.js 15 (v16.1.6) project in /inv directory using Bun
- Removed default ESLint config (eslint.config.mjs) — will be replaced with Oxlint in 1.2
- Replaced default page.tsx with simple "Hello inv." placeholder
- Stripped globals.css down to just `@import "tailwindcss"`
- Updated dev script to use --turbopack
- Removed broken lint script (ESLint config removed, Oxlint not yet added)
- Verified: `bun run build` succeeds, TypeScript compiles clean

Next up:
- Feature 1.2: Configure tsgo, Oxlint, and Oxfmt
- Feature 1.3: Initialize shadcn/ui and configure DM Sans font
- Feature 1.4: Set up Drizzle ORM with Neon Postgres

Notes:
- The .env at project root has a real Neon DATABASE_URL. Other secrets are placeholders.
- ESLint + eslint-config-next are still in devDependencies — remove them when adding Oxlint in 1.2.

## 2026-02-18 — Feature 1.4: Set up Drizzle ORM with Neon Postgres

Completed:
- Installed drizzle-orm (v0.45.1) and @neondatabase/serverless (v1.0.2) as dependencies
- Installed drizzle-kit (v0.31.9) as dev dependency
- Created inv/.env with DATABASE_URL (real Neon connection), SESSION_SECRET, RESEND_API_KEY, NEXT_PUBLIC_URL, and empty STRIPE placeholders
- Created drizzle.config.ts pointing to schema at src/lib/db/schema.ts, output to drizzle/, postgresql dialect
- Created src/lib/db/index.ts — initializes Neon HTTP client and exports drizzle instance
- Created src/lib/db/schema.ts — placeholder for Phase 2 schema definitions
- Added package.json scripts: db:generate, db:migrate, db:studio
- Verified: TypeScript compiles clean, `bun run build` succeeds, `bun run db:studio` launches and connects to Neon

Prioritization note:
- Chose 1.4 over 1.2 and 1.3 because Drizzle/DB setup unblocks Phase 2 (schemas) and all subsequent functionality. Linting (1.2) and UI fonts (1.3) are non-blocking.

Next up:
- Feature 1.2: Configure tsgo, Oxlint, and Oxfmt (non-blocking, but needed for code quality)
- Feature 1.3: Initialize shadcn/ui and configure DM Sans font (needed before UI work in Phase 3+)
- Feature 2.1+: Database schema definitions (now unblocked by 1.4)

Notes:
- inv/.env is gitignored (via `.env*` pattern in .gitignore) — safe
- The neon driver uses HTTP mode (neon-http), suitable for serverless/edge environments
- ESLint + eslint-config-next still in devDependencies — remove when adding Oxlint in 1.2

## 2026-02-18 — Feature 2.1: Create users table schema

Completed:
- Defined users table in src/lib/db/schema.ts with all columns from PRD:
  - id (uuid, defaultRandom, primaryKey)
  - email (text, notNull, unique)
  - name (text, notNull)
  - passwordHash (text, notNull) — mapped to snake_case "password_hash" column
  - businessName, businessAddress, vatNumber, bankDetails, logoUrl (all optional text)
  - defaultCurrency (text, default 'EUR')
  - invoicePrefix (text, default 'INV')
  - nextInvoiceNumber (integer, default 1)
  - createdAt (timestamp, defaultNow)
- All column names use snake_case in the DB via explicit column name strings
- TypeScript compiles clean, `bun run build` succeeds

Prioritization note:
- Chose 2.1 (users schema) as highest priority because it's the foundational table referenced by clients, invoices, and all subsequent features. Without the users table, nothing in Phase 2-10 can proceed. Features 1.2 (Oxlint) and 1.3 (shadcn/ui) are tooling/UI that don't block functionality.

Next up:
- Feature 2.2: Create clients table schema (references users)
- Feature 2.3: Create invoices and line_items table schemas
- Feature 2.4: Activity log table and Drizzle relations
- Feature 2.5: Generate and run initial migration

Notes:
- Using drizzle-orm 0.45.1 pg-core API
- ESLint + eslint-config-next still in devDependencies — remove when adding Oxlint in 1.2

## 2026-02-18 — Feature 2.2: Create clients table schema

Completed:
- Added clients table to src/lib/db/schema.ts with all columns from PRD:
  - id (uuid, defaultRandom, primaryKey)
  - userId (uuid, notNull, references users.id) — mapped to snake_case "user_id"
  - name (text, notNull)
  - email (text, notNull)
  - company (text, optional)
  - address (text, optional)
  - vatNumber (text, optional) — mapped to snake_case "vat_number"
  - notes (text, optional)
  - defaultCurrency (text, default 'EUR') — mapped to snake_case "default_currency"
  - createdAt (timestamp, defaultNow) — mapped to snake_case "created_at"
  - updatedAt (timestamp, defaultNow) — mapped to snake_case "updated_at"
- Foreign key: userId references users.id
- TypeScript compiles clean, `bun run build` succeeds

Prioritization note:
- Chose 2.2 (clients schema) as highest priority because it's the next table on the critical path. Invoices (2.3) references clients, and all functional features (Phase 3+) depend on the schema being complete. Features 1.2 (Oxlint) and 1.3 (shadcn/ui) remain non-blocking tooling/UI setup.

Next up:
- Feature 2.3: Create invoices and line_items table schemas (now unblocked by 2.2)
- Feature 2.4: Activity log table and Drizzle relations
- Feature 2.5: Generate and run initial migration

Notes:
- All column names use snake_case in the DB via explicit column name strings (same pattern as users table)
- ESLint + eslint-config-next still in devDependencies — remove when adding Oxlint in 1.2

## 2026-02-18 — Feature 2.3: Create invoices and line_items table schemas

Completed:
- Added invoiceStatusEnum pgEnum with values: draft, sent, paid, overdue, cancelled
- Added invoices table to src/lib/db/schema.ts with all columns from PRD:
  - id (uuid, defaultRandom, primaryKey)
  - userId (uuid, notNull, references users.id) — mapped to "user_id"
  - clientId (uuid, notNull, references clients.id) — mapped to "client_id"
  - number (text, notNull, unique)
  - status (invoiceStatusEnum, default 'draft')
  - currency (text, default 'EUR')
  - issuedAt (date, notNull) — mapped to "issued_at"
  - dueAt (date, notNull) — mapped to "due_at"
  - paidAt (date) — mapped to "paid_at"
  - sentAt (timestamp) — mapped to "sent_at"
  - subtotal (numeric 12,2)
  - taxRate (numeric 5,2) — mapped to "tax_rate"
  - taxAmount (numeric 12,2) — mapped to "tax_amount"
  - total (numeric 12,2)
  - stripePaymentLinkId, stripePaymentLinkUrl, stripePaymentIntentId (text, optional)
  - notes, internalNotes (text, optional)
  - reverseCharge (text, default 'false')
  - createdAt, updatedAt (timestamp, defaultNow)
- Added lineItems table (mapped to "line_items") with all columns from PRD:
  - id (uuid, defaultRandom, primaryKey)
  - invoiceId (uuid, notNull, references invoices.id, onDelete cascade) — mapped to "invoice_id"
  - description (text, notNull)
  - quantity (numeric 10,2, notNull)
  - unitPrice (numeric 12,2, notNull) — mapped to "unit_price"
  - amount (numeric 12,2, notNull)
  - sortOrder (integer, default 0) — mapped to "sort_order"
- TypeScript compiles clean, `bun run build` succeeds

Prioritization note:
- Chose 2.3 (invoices + line_items schemas) as highest priority because invoices are the core domain entity. Everything from Phase 3 onward depends on the schema being complete. Features 1.2 (Oxlint) and 1.3 (shadcn/ui) remain non-blocking.

Next up:
- Feature 2.4: Activity log table and Drizzle relations
- Feature 2.5: Generate and run initial migration

Notes:
- numeric columns use drizzle-orm v0.45.1 config syntax: numeric({ precision: X, scale: Y }) or numeric("col_name", { precision: X, scale: Y })
- lineItems has onDelete: "cascade" on invoiceId FK — deleting an invoice cascades to its line items
- ESLint + eslint-config-next still in devDependencies — remove when adding Oxlint in 1.2

## 2026-02-18 — Feature 2.4: Create activity_log table and Drizzle relations

Completed:
- Added activityLog table to src/lib/db/schema.ts with all columns from PRD:
  - id (uuid, defaultRandom, primaryKey)
  - invoiceId (uuid, references invoices.id) — mapped to "invoice_id"
  - action (text, notNull)
  - metadata (jsonb)
  - createdAt (timestamp, defaultNow) — mapped to "created_at"
- Imported `relations` from drizzle-orm and `jsonb` from drizzle-orm/pg-core
- Defined all Drizzle relations:
  - usersRelations: users hasMany clients, users hasMany invoices
  - clientsRelations: clients belongsTo user, clients hasMany invoices
  - invoicesRelations: invoices belongsTo user, invoices belongsTo client, invoices hasMany lineItems, invoices hasMany activityLog
  - lineItemsRelations: lineItems belongsTo invoice
  - activityLogRelations: activityLog belongsTo invoice
- TypeScript compiles clean, `bun run build` succeeds

Prioritization note:
- Chose 2.4 (activity_log + relations) as highest priority because it completes the entire Phase 2 database schema. Feature 2.5 (generate and run migration) depends on having all tables and relations defined. Features 1.2 (Oxlint) and 1.3 (shadcn/ui) remain non-blocking tooling/UI setup.

Next up:
- Feature 2.5: Generate and run initial migration (now unblocked — all 5 tables are defined)
- Feature 1.2: Configure tsgo, Oxlint, and Oxfmt (non-blocking, but needed for code quality)
- Feature 1.3: Initialize shadcn/ui and configure DM Sans font (needed before UI work in Phase 3+)

Notes:
- All 5 tables now defined: users, clients, invoices, lineItems, activityLog
- Relations enable Drizzle's relational query API (db.query.invoices.findMany({ with: { client: true, lineItems: true } }))
- ESLint + eslint-config-next still in devDependencies — remove when adding Oxlint in 1.2

## 2026-02-18 — Feature 2.5: Generate and run initial migration

Completed:
- Ran `bun run db:generate` — generated migration file drizzle/0000_classy_chamber.sql
- Verified generated SQL creates all 5 tables (users, clients, invoices, line_items, activity_log) with correct columns, types, defaults, constraints, and foreign keys
- Verified invoice_status enum is created with values: draft, sent, paid, overdue, cancelled
- Verified foreign keys: clients→users, invoices→users, invoices→clients, line_items→invoices (CASCADE), activity_log→invoices
- Verified unique constraints on users.email and invoices.number
- Ran `bun run db:migrate` — all migrations applied successfully to Neon Postgres
- Verified `bun run build` still succeeds after migration

Prioritization note:
- Chose 2.5 (generate and run migration) as highest priority because all 5 tables were defined (2.1–2.4 complete) but not yet created in the database. Without the migration, no Phase 3+ features (auth, CRUD, UI) can function. This was the critical-path bottleneck.

Next up:
- Feature 1.3: Initialize shadcn/ui and configure DM Sans font (needed before any UI work in Phase 3+)
- Feature 1.2: Configure tsgo, Oxlint, and Oxfmt (non-blocking but needed for code quality)
- Feature 3.1+: Auth & Layout (now unblocked — database tables exist)

Notes:
- Migration file: drizzle/0000_classy_chamber.sql
- Migration metadata: drizzle/meta/_journal.json and drizzle/meta/0000_snapshot.json
- The drizzle/ directory contains generated files that should be committed (they track migration state)
- ESLint + eslint-config-next still in devDependencies — remove when adding Oxlint in 1.2

## 2026-02-18 — Feature 3.1: Implement session-based auth middleware

Completed:
- Created src/middleware.ts implementing session-based route protection
- Defined PUBLIC_PATHS array: ['/login', '/api/webhooks'] — these bypass auth
- Middleware checks for 'session' cookie, compares value to SESSION_SECRET env var
- Invalid/missing session redirects to /login
- Config matcher uses negative lookahead regex to exclude _next/static, _next/image, favicon.ico
- TypeScript compiles clean, `bun run build` succeeds

Prioritization note:
- Chose 3.1 (auth middleware) as highest priority because it's the gateway to the entire authenticated app. All dashboard pages (Phases 3-11) will be protected by this middleware. Without auth, the app has no security boundary. Features 1.2 (Oxlint) and 1.3 (shadcn/ui) remain non-blocking setup tasks.

Next up:
- Feature 1.3: Initialize shadcn/ui and configure DM Sans font (needed before UI work — 3.2, 3.3, 3.4 all require shadcn components)
- Feature 3.2: Create login page and auth action (needs shadcn for UI components)
- Feature 3.3: Create dashboard layout with header and navigation

Notes:
- Next.js 16.1.6 shows a deprecation warning: "The 'middleware' file convention is deprecated. Please use 'proxy' instead." However, middleware still works and is recognized as "ƒ Proxy (Middleware)" in the build output. The PRD specifies middleware, and it functions correctly.
- No test framework is configured yet — verification is via TypeScript compilation and build success
- ESLint + eslint-config-next still in devDependencies — remove when adding Oxlint in 1.2

## 2026-02-19 — Feature 1.3: Initialize shadcn/ui and configure DM Sans font

Completed:
- Initialized shadcn/ui using `bunx shadcn@latest init -d -b zinc --css-variables`
  - Style: New York, Base color: Zinc, CSS variables: enabled
  - Generated components.json config, utils.ts (with cn() helper), and globals.css theme
- Installed 12 shadcn/ui components: button, input, label, select, table, badge, separator, dropdown-menu, dialog, sheet, tabs, card
- Installed dependencies: clsx, tailwind-merge, class-variance-authority, lucide-react, tw-animate-css, @radix-ui/* components
- Replaced Geist/Geist_Mono fonts with DM Sans and DM Mono in src/app/layout.tsx
  - DM_Sans with --font-sans CSS variable
  - DM_Mono (weights 400, 500) with --font-mono CSS variable
- Added --font-sans and --font-mono to Tailwind v4 @theme inline block in globals.css
- Added `font-sans` to body base styles so DM Sans is the default body font
- Updated metadata: title "inv.", description "Personal invoicing app"
- TypeScript compiles clean, `bun run build` succeeds

Prioritization note:
- Chose 1.3 (shadcn/ui + DM Sans) as highest priority because it's the critical blocker for ALL UI work. Every remaining feature from 3.2 onward needs shadcn components (Button, Input, Dialog, etc.) and the design system (zinc palette, DM Sans font). Feature 1.2 (Oxlint/Oxfmt) is non-blocking tooling.

Next up:
- Feature 3.2: Create login page and auth action (now unblocked — shadcn components available)
- Feature 3.3: Create dashboard layout with header and navigation
- Feature 3.4: Create shared UI components (StatusBadge, StatCard)
- Feature 1.2: Configure tsgo, Oxlint, and Oxfmt (non-blocking tooling)

Notes:
- shadcn/ui is configured for Tailwind v4 (CSS-based theme, no tailwind.config.ts)
- The @theme inline block in globals.css uses oklch color values (zinc palette)
- All 12 component files live in src/components/ui/
- ESLint + eslint-config-next still in devDependencies — remove when adding Oxlint in 1.2

## 2026-02-19 — Feature 3.2: Create login page and auth action

Completed:
- Created src/actions/auth.ts with 'use server' directive
  - login() action: accepts FormData via useActionState pattern (prevState, formData)
  - Password verification uses Node.js crypto scrypt + timingSafeEqual
  - Expects PASSWORD_HASH env var in format "salt:scrypt_hex" (64-byte key length)
  - On success: sets httpOnly "session" cookie with SESSION_SECRET value, redirects to /invoices
  - On failure: returns { error: string } for client-side display
  - Cookie config: httpOnly, secure in production, sameSite lax, 30-day maxAge
  - logout() action: deletes session cookie, redirects to /login
- Created src/app/login/page.tsx as Client Component
  - Minimal centered layout: logo mark (black rounded square with "inv"), title "inv."
  - Single password field + "Sign in" button, styled with shadcn Input, Label, Button
  - Uses React 19 useActionState for form submission with pending state
  - Shows error message in red below the form on auth failure
  - Button shows "Signing in..." while pending
- TypeScript compiles clean, `bun run build` succeeds
- Login page renders at /login as static route

Prioritization note:
- Chose 3.2 (login page + auth action) as highest priority because the auth middleware (3.1) is done but there's no way for users to actually authenticate. Without login, the entire app is inaccessible — every dashboard feature (3.3, 3.4, 4.x, 5.x, ...) is untestable. This is the critical-path bottleneck.

Next up:
- Feature 3.3: Create dashboard layout with header and navigation (next on critical path)
- Feature 3.4: Create shared UI components (StatusBadge, StatCard)
- Feature 4.1+: Client CRUD (needs dashboard layout first)

Notes:
- PASSWORD_HASH env var must be added to .env. Generate with:
  node -e "const c=require('crypto'),s=c.randomBytes(16).toString('hex');c.scrypt('your-password',s,64,(e,k)=>console.log(s+':'+k.toString('hex')))"
- The middleware (3.1) compares cookie value directly to SESSION_SECRET — the login action sets that exact value
- /invoices doesn't exist yet (Phase 5), so after login you'll get a 404 — this is expected until 5.2 is built
- ESLint + eslint-config-next still in devDependencies — remove when adding Oxlint in 1.2
- No test framework configured yet — verification is via TypeScript compilation and build success

## 2026-02-19 — Feature 3.3: Create dashboard layout with header and navigation

Completed:
- Created src/app/(dashboard)/layout.tsx as a Client Component (uses usePathname for active tab)
- Header: left side has logo (small black rounded square with "inv" text + "inv." label), nav tabs for Invoices and Clients
- Right side: "New Invoice" button (default/black, with Plus icon from lucide-react)
- Header has bottom border (border-zinc-100)
- Main content area: max-w-6xl mx-auto px-6 py-8
- Nav tabs use Next.js Link components to /invoices and /clients
- Active tab highlighted with bg-zinc-100 + text-zinc-900; inactive tabs text-zinc-500 with hover state
- Created placeholder pages: src/app/(dashboard)/invoices/page.tsx and src/app/(dashboard)/clients/page.tsx
- TypeScript compiles clean, `bun run build` succeeds
- All routes render: /, /clients, /invoices, /login

Prioritization note:
- Chose 3.3 (dashboard layout) as highest priority because it's the gateway to ALL dashboard functionality. Every remaining feature (3.4, 4.x, 5.x, ..., 11.x) renders inside this layout. Without it, no dashboard page can exist. It was the single biggest blocker on the critical path.

Next up:
- Feature 3.4: Create shared UI components (StatusBadge, StatCard) — needed by invoice list (5.2)
- Feature 4.1: Client CRUD server actions — next on critical path after 3.4
- Feature 1.2: Configure tsgo, Oxlint, and Oxfmt (non-blocking tooling)

Notes:
- Login redirects to /invoices which now renders (placeholder) inside the dashboard layout
- The layout is a Client Component because usePathname() requires it — this is standard Next.js pattern
- ESLint + eslint-config-next still in devDependencies — remove when adding Oxlint in 1.2
- No test framework configured yet — verification is via TypeScript compilation and build success

## 2026-02-19 — Feature 4.1: Create server actions for client CRUD

Completed:
- Created src/actions/clients.ts with 'use server' directive
- Installed zod@4.3.6 for schema validation
- Defined clientSchema with zod: name (string, min 1), email (string, email), company (optional), address (optional), vatNumber (optional), notes (optional), defaultCurrency (optional, default 'EUR')
- Implemented getUserId() helper: queries first user from database (single-user app, will be replaced with proper auth user lookup in 10.2)
- Implemented createClient action: uses useActionState pattern (prevState, formData), validates with zod, inserts into clients table with userId, revalidates /clients
- Implemented updateClient action: accepts clientId as first arg, validates, updates by id, sets updatedAt to current timestamp, revalidates /clients and /clients/[id]
- Implemented deleteClient action: deletes by id, revalidates /clients
- TypeScript compiles clean, `bun run build` succeeds

Prioritization note:
- Chose 4.1 (client CRUD actions) as highest priority because clients must exist before invoices can be created (invoices reference clients via clientId FK). This is the critical-path bottleneck — all of Phase 4 (client UI) and Phase 5+ (invoice CRUD) depend on it. Features 1.2 (Oxlint) and 3.4 (StatusBadge/StatCard) are non-blocking.

Next up:
- Feature 4.2: Build clients list page (now unblocked by 4.1)
- Feature 4.3: Build client form dialog and detail page
- Feature 3.4: Create shared UI components (StatusBadge, StatCard) — needed for 5.2

Notes:
- Using zod@4.3.6 (Zod 4) — import as `import { z } from "zod"`
- getUserId() queries first user from DB; will be replaced with getCurrentUser() in Feature 10.2
- createClient and updateClient use useActionState-compatible signatures (prevState, formData)
- ESLint + eslint-config-next still in devDependencies — remove when adding Oxlint in 1.2
- No test framework configured yet — verification is via TypeScript compilation and build success

## 2026-02-19 — Feature 4.2: Build clients list page

Completed:
- Replaced placeholder clients page with full Server Component at src/app/(dashboard)/clients/page.tsx
- Queries all clients from DB ordered by name, with aggregated invoice stats via LEFT JOIN
  - Uses drizzle-orm SQL builder: db.select().from(clients).leftJoin(invoices, ...).groupBy(clients.id)
  - Computes invoiceCount (count) and totalInvoiced (sum) per client
- Each client rendered as a clickable row (Link to /clients/[id]):
  - Left side: avatar circle with initials (zinc-100 bg, zinc-600 text), name + email
  - Right side: total invoiced amount (formatted with Intl.NumberFormat) + invoice count
- "New Client" button in top right linking to /clients/new
- Clean styling: divide-y dividers, hover:bg-zinc-50 transitions, generous padding
- Empty state: "No clients yet." when no clients exist
- Helper functions: getInitials(name) for avatar, formatCurrency(amount, currency) for display
- TypeScript compiles clean, bun run build succeeds

Prioritization note:
- Chose 4.2 (clients list page) as highest priority because client CRUD actions (4.1) are done but there's no UI to see or interact with clients. Without a visible client list, client management is untestable, and Phase 5 (invoices) depends on being able to verify clients exist. This is the critical-path bottleneck.

Next up:
- Feature 4.3: Build client form dialog and detail page (now the top priority — needed to actually create/edit/delete clients)
- Feature 3.4: Create shared UI components (StatusBadge, StatCard) — needed for 5.2
- Feature 5.1+: Invoice CRUD server actions

Notes:
- /clients/new and /clients/[id] routes don't exist yet — will 404 until Feature 4.3 is built
- formatCurrency is defined inline in the page; Feature 3.4 will add a shared version to lib/utils.ts
- PostgreSQL functional dependency grouping allows SELECT of non-aggregated columns when GROUP BY includes the primary key
- ESLint + eslint-config-next still in devDependencies — remove when adding Oxlint in 1.2
- No test framework configured yet — verification is via TypeScript compilation and build success

## 2026-02-19 — Feature 4.3: Build client form dialog and detail page

Completed:
- Created src/components/client-form.tsx — reusable Client Component form with all fields:
  - name (required), email (required), company, address, vatNumber, notes, defaultCurrency (Select: EUR/USD/GBP)
  - Uses useActionState for form submission with pending state
  - Accepts action prop (useActionState-compatible), defaultValues for editing, submitLabel, redirectTo, onSuccess, onCancel
  - Redirects via useRouter on success when redirectTo is provided
  - Calls onSuccess callback on success (used by edit mode to toggle back to view)
  - Uses shadcn Input, Label, Select components in a responsive 2-column grid
- Created src/app/(dashboard)/clients/new/page.tsx — Client Component page
  - Back link to /clients, heading "New Client"
  - Renders ClientForm with createClient action, redirects to /clients on success
- Created src/app/(dashboard)/clients/[id]/page.tsx — Server Component
  - Queries client by id from DB, returns 404 if not found
  - Queries client's invoices ordered by createdAt desc
  - Passes data to ClientDetailView client component
- Created src/components/client-detail.tsx — Client Component for interactive detail view
  - Header: avatar with initials, client name, company + email
  - Edit button toggles between detail view and edit form (ClientForm with updateClient.bind(null, clientId))
  - Detail view shows address, VAT number, currency, notes in a 2-column grid
  - Invoice list: shows each invoice as a clickable row with number, status badge (color-coded), total, due date
  - Delete button with Dialog confirmation — calls deleteClient and redirects to /clients
  - Empty state for invoices: "No invoices for this client."
- TypeScript compiles clean, `bun run build` succeeds
- Routes: /clients/new (static), /clients/[id] (dynamic)

Prioritization note:
- Chose 4.3 (client form + detail page) as highest priority because it's the critical-path bottleneck. Without it, no clients can be created (the /clients/new page didn't exist), and without clients, no invoices can be created (invoices require a clientId FK). This blocks all of Phase 5+ (invoice CRUD, PDF generation, Stripe, email, etc.).

Next up:
- Feature 3.4: Create shared UI components (StatusBadge, StatCard) — needed for 5.2 (invoice list page)
- Feature 5.1: Create server actions for invoice CRUD — next on critical path
- Feature 1.2: Configure tsgo, Oxlint, and Oxfmt (non-blocking tooling)

Notes:
- updateClient uses .bind(null, clientId) to create a useActionState-compatible action
- ClientForm uses Radix Select with name prop for form submission (renders hidden native select)
- formatCurrency is duplicated in client-detail.tsx (same as clients list page); Feature 3.4 will add shared version to lib/utils.ts
- ESLint + eslint-config-next still in devDependencies — remove when adding Oxlint in 1.2
- No test framework configured yet — verification is via TypeScript compilation and build success

## 2026-02-19 — Feature 5.1: Create server actions for invoice CRUD

Completed:
- Created src/actions/invoices.ts with 'use server' directive
- Defined invoiceSchema with zod: clientId (uuid), currency (string, default EUR), issuedAt (string), dueAt (string), taxRate (coerced number, 0-100, default 0), notes (optional), internalNotes (optional), reverseCharge (string, default "false"), lineItems array of { description, quantity, unitPrice }
- Defined lineItemSchema with zod: description (string, min 1), quantity (coerced positive number), unitPrice (coerced non-negative number)
- Implemented getUserId() helper (same pattern as clients.ts — queries first user)
- Implemented calculateTotals(): computes subtotal from line items, taxAmount from subtotal * taxRate/100, total as subtotal + taxAmount; returns all as .toFixed(2) strings for numeric columns
- Implemented parseInvoiceFormData(): extracts fields from FormData, parses lineItems from JSON string
- Implemented createInvoice action (useActionState-compatible: prevState, formData):
  - Validates with zod
  - Runs in a db.transaction() for atomicity:
    - Queries user's invoicePrefix + nextInvoiceNumber
    - Generates invoice number as "PREFIX-0042" (padded to 4 digits)
    - Atomically increments nextInvoiceNumber on users table
    - Inserts invoice with calculated totals
    - Inserts all line items with sortOrder
    - Inserts activityLog entry with action "created"
  - Returns { success: true, invoiceId } on success
- Implemented updateInvoice action (invoiceId, prevState, formData):
  - Validates with zod
  - Runs in a db.transaction():
    - Updates invoice row with new data + totals + updatedAt
    - Deletes old line items, inserts new ones (replace strategy)
  - Revalidates /invoices and /invoices/[id]
- Implemented deleteInvoice action: deletes by id (line items cascade via FK), revalidates /invoices
- TypeScript compiles clean, `bun run build` succeeds

Prioritization note:
- Chose 5.1 (invoice CRUD actions) as highest priority because invoices are the core domain entity of the entire app. Without invoice CRUD, nothing in Phase 5 through 11 can proceed — no invoice list (5.2), no invoice form (5.3), no PDF generation (6.x), no Stripe integration (7.x), no email sending (8.x). This is the critical-path bottleneck. Feature 3.4 (StatusBadge/StatCard) is needed for UI but doesn't block the server actions.

Next up:
- Feature 3.4: Create shared UI components (StatusBadge, StatCard, formatCurrency) — needed for 5.2 (invoice list page)
- Feature 5.2: Build invoice list page with stats and filters (needs 3.4 + 5.1)
- Feature 5.3: Build invoice creation form (needs 5.1)

Notes:
- Line items are passed as a JSON string in FormData (field name "lineItems") — this is the cleanest approach for dynamic arrays in server actions
- Numeric columns (subtotal, taxRate, taxAmount, total, quantity, unitPrice, amount) are stored as .toFixed(2) strings matching the numeric(precision, scale) column types
- Invoice number generation is atomic within the transaction to prevent duplicates
- updateInvoice uses a delete-then-insert strategy for line items (simpler than diffing)
- db.transaction() works with drizzle-orm/neon-http (Neon HTTP transactions)
- ESLint + eslint-config-next still in devDependencies — remove when adding Oxlint in 1.2
- No test framework configured yet — verification is via TypeScript compilation and build success

## 2026-02-19 — Feature 3.4: Create shared UI components (StatusBadge, StatCard, formatCurrency)

Completed:
- Created src/components/status-badge.tsx — renders invoice status as a styled pill
  - Colors: paid=emerald, sent=blue, draft=zinc, overdue=red, cancelled=zinc
  - Simple component accepting { status: string }
- Created src/components/stat-card.tsx — renders label (xs, uppercase, zinc-400), value (2xl, semibold), and optional sub text (xs, zinc-400)
  - Accepts { label, value, sub? } props
- Added formatCurrency(amount, currency) to src/lib/utils.ts using Intl.NumberFormat
- Removed duplicate formatCurrency from src/app/(dashboard)/clients/page.tsx — now imports from @/lib/utils
- Removed duplicate formatCurrency and statusColors from src/components/client-detail.tsx — now imports formatCurrency from @/lib/utils and uses StatusBadge component
- TypeScript compiles clean, `bun run build` succeeds

Prioritization note:
- Chose 3.4 (shared UI components) as highest priority because it's the direct blocker for Feature 5.2 (invoice list page with stats and filters). StatCard is needed for the stats row, StatusBadge is needed for the invoice table, and formatCurrency was duplicated in two files. Without these components, the main invoice dashboard page cannot be built. All remaining Phase 5 UI features (5.2, 5.3, 5.4) depend on these shared components.

Next up:
- Feature 5.2: Build invoice list page with stats and filters (now unblocked by 3.4 + 5.1)
- Feature 5.3: Build invoice creation form (needs 5.1, which is done)
- Feature 5.4: Build invoice detail page

Notes:
- StatusBadge uses plain span with Tailwind classes (not shadcn Badge) to keep it simple and match the inline styles that were already in client-detail.tsx
- formatCurrency is now the single source of truth in src/lib/utils.ts
- client-detail.tsx now uses StatusBadge for invoice status display
- ESLint + eslint-config-next still in devDependencies — remove when adding Oxlint in 1.2
- No test framework configured yet — verification is via TypeScript compilation and build success

## 2026-02-19 — Feature 5.2: Build invoice list page with stats and filters

Completed:
- Replaced placeholder invoices page with full Server Component at src/app/(dashboard)/invoices/page.tsx
- Queries all invoices with client data via LEFT JOIN on clients table, ordered by issuedAt desc
- Computes stats from all invoices (regardless of filter):
  - Outstanding: sum of total for sent + overdue invoices
  - Total Paid: sum of total for paid invoices
  - Overdue: sum of total for overdue invoices + count
- Renders 3 StatCard components at top with computed values
- Filter tabs: All, Draft, Sent, Paid, Overdue — uses URL search params (?status=draft) for filter state
  - Active tab highlighted with bg-zinc-100 + text-zinc-900
  - "All" tab links to /invoices (no query param)
- Invoice table with columns: Number (mono font), Client (name + email), Amount (right-aligned, semibold), Status (StatusBadge), Issued, Due
- Rows link to /invoices/[id] (will 404 until Feature 5.4 is built)
- Empty state: "No invoices yet." when no invoices match filter
- formatDate helper for displaying dates in "Mon DD, YYYY" format
- TypeScript compiles clean, `bun run build` succeeds
- Route renders as dynamic (ƒ) due to searchParams usage

Prioritization note:
- Chose 5.2 (invoice list page) as highest priority because it's the main invoice dashboard — the first thing users see after login. Server actions (5.1) and shared components (3.4) were already done, making it fully unblocked. Without this page, users can't see, navigate to, or manage any invoices. This is the critical-path bottleneck for all remaining invoice features (5.3, 5.4, 5.5) and beyond.

Next up:
- Feature 5.3: Build invoice creation form (needs 5.1, which is done)
- Feature 5.4: Build invoice detail page with side panel or full page view
- Feature 5.5: Implement invoice duplication

Notes:
- Stats are computed in JS from the full invoice list (not separate SQL aggregates) — fine for a single-user app with small dataset
- /invoices/[id] doesn't exist yet — will 404 until Feature 5.4 is built
- ESLint + eslint-config-next still in devDependencies — remove when adding Oxlint in 1.2
- No test framework configured yet — verification is via TypeScript compilation and build success

## 2026-02-19 — Feature 5.3: Build invoice creation form

Completed:
- Created src/components/invoice-form.tsx — reusable Client Component with full invoice form:
  - Client selector dropdown (populated from DB via server component)
  - Currency select (EUR/USD/GBP) with live state
  - Issue date (defaults to today) and due date (defaults to 30 days from today)
  - Dynamic line items section with add/remove rows:
    - Each row: Description (required), Qty/Hrs (number), Rate (number), auto-calculated Amount
    - Trash icon to remove rows (disabled when only 1 row)
    - "+Add line item" button below the table
  - Live total calculation: subtotal, tax rate (editable %) with computed tax amount, total
  - Notes field (visible on invoice) and Internal Notes field
  - Reverse charge checkbox
  - Action buttons: Cancel (navigates to /invoices) and Save Draft (submits form)
- Created src/app/(dashboard)/invoices/new/page.tsx — Server Component
  - Fetches all clients from DB ordered by name
  - Passes clients list and createInvoice server action to InvoiceForm
  - Back link to /invoices, heading "New Invoice"
- Form uses useActionState with createInvoice server action
  - Line items serialized as JSON in FormData (matching server action's parseInvoiceFormData)
  - Currency and taxRate managed as controlled state for live total calculation
  - On success, redirects to /invoices
- TypeScript compiles clean, `bun run build` succeeds
- Route: /invoices/new renders as static page

Prioritization note:
- Chose 5.3 (invoice creation form) as highest priority because invoice CRUD actions (5.1) and the list page (5.2) are done but there's no way to actually create invoices through the UI. Without this form, the entire app is non-functional for its core purpose — it's an invoicing app with no way to create invoices. Everything downstream (5.4 detail page, 5.5 duplication, 6.x PDF, 7.x Stripe, 8.x email) depends on invoices existing. This is the critical-path bottleneck.

Next up:
- Feature 5.4: Build invoice detail page (now the top priority — needed to view/manage created invoices)
- Feature 5.5: Implement invoice duplication
- Feature 1.2: Configure tsgo, Oxlint, and Oxfmt (non-blocking tooling)

Notes:
- The "Send Invoice" button is not included yet — it requires Phase 7 (Stripe) and Phase 8 (email). Save Draft is the only submit action for now.
- The "Enable Stripe payment link" checkbox from the PRD is deferred to Phase 7 when Stripe is integrated.
- InvoiceForm is designed for reuse by Feature 5.4's edit page — accepts defaultValues prop and configurable action/submitLabel.
- /invoices/[id] doesn't exist yet — will 404 until Feature 5.4 is built.
- ESLint + eslint-config-next still in devDependencies — remove when adding Oxlint in 1.2
- No test framework configured yet — verification is via TypeScript compilation and build success

## 2026-02-19 — Feature 1.2: Configure tsgo, Oxlint, and Oxfmt

Completed:
- Added package.json scripts: tsc (tsgo), lint (oxlint), format (oxfmt --write .), format:check (oxfmt --check .), check (npm-run-all --parallel tsc lint format:check)
- Created oxlint.config.ts with @nkzw/oxlint-config extending via defineConfig from oxlint
- Created .oxfmtrc.jsonc with experimentalSortImports and experimentalTailwindcss (pointed at globals.css stylesheet for Tailwind v4, with cn/clsx/cva function support)
- Created .vscode/settings.json with tsgo, oxc-vscode formatter, formatOnSave, and fixAll.oxc
- Removed eslint and eslint-config-next from devDependencies
- Ran oxfmt to format the entire codebase (sorted imports, formatted all 46 files)
- Fixed 27 lint errors across 13 files:
  - Replaced `import * as React from "react"` with `import type { ComponentProps } from "react"` in all 12 shadcn UI components
  - Replaced `React.ComponentProps<...>` with `ComponentProps<...>` throughout
  - Sorted CVA variant objects alphabetically in button.tsx and badge.tsx (perfectionist/sort-objects)
  - Changed `isNaN()` to `Number.isNaN()` in invoice-form.tsx (unicorn/prefer-number-properties)
  - Changed `Client[]` to `Array<Client>` and similar in invoice-form.tsx (typescript-eslint/array-type)
- All three checks pass: `bun run check` (tsc + lint + format:check) — 0 errors
- `bun run build` succeeds

Prioritization note:
- Chose 1.2 (tsgo/Oxlint/Oxfmt) as highest priority because (1) the task workflow requires running `bun format` and `bun check` which didn't exist as scripts, (2) this feature had been deferred 15+ consecutive sessions creating growing code quality debt, and (3) the dependencies were already installed — only config and scripts were missing. Every future feature benefits from having automated code quality checks.

Next up:
- Feature 5.4: Build invoice detail page (critical path — blocks 5.5, 6.2, 8.2, 9.2, 11.3)
- Feature 5.5: Implement invoice duplication
- Feature 6.1: Install react-pdf and create PDF invoice template

Notes:
- PRD specified `organizeImports` and `tailwindCSS.sortClasses` for .oxfmtrc.jsonc, but oxfmt 0.33.0 uses `experimentalSortImports` and `experimentalTailwindcss` — updated to match actual API
- PRD specified scripts as `fmt` and `fmt:check` but used `format` and `format:check` to match the task workflow's `bun format` command
- oxlint.config.ts triggers a Node.js MODULE_TYPELESS_PACKAGE_JSON warning since package.json lacks `"type": "module"`. Cosmetic only — doesn't affect functionality.
- The @nkzw/oxlint-config enables strict perfectionist rules (sort-objects, sort-jsx-props, sort-object-types) which enforce alphabetical ordering of all object properties and JSX attributes
- ESLint is now fully removed from devDependencies
- No test framework configured yet

## 2026-02-19 — Feature 5.4: Build invoice detail page with side panel or full page view

Completed:
- Created src/components/invoice-detail.tsx — Client Component for interactive invoice detail view:
  - Header: invoice number (mono font) + StatusBadge, client name + email, large total amount
  - Action buttons based on status:
    - Draft: "Send Invoice" (disabled, Phase 8) + "Edit" (links to edit page)
    - Sent: "Send Reminder" (disabled, Phase 9)
    - Overdue: "Send Overdue Notice" (disabled, Phase 9)
    - All statuses: "Download PDF" (disabled, Phase 6) + "Delete" (with confirmation dialog)
  - Stripe payment link indicator for sent invoices with active payment links
  - Invoice details grid: issue date, due date, paid date (or currency)
  - Reverse charge indicator when applicable
  - Line items table with Description, Qty, Rate, Amount columns inside bordered card
  - Totals section: subtotal, tax (if > 0%), total with separator
  - Notes and internal notes sections
  - Delete confirmation dialog (same pattern as client-detail.tsx)
- Created src/app/(dashboard)/invoices/[id]/page.tsx — Server Component
  - Queries invoice by id, returns 404 if not found
  - Queries client (name, email) by invoice.clientId
  - Queries line items ordered by sortOrder
  - Passes assembled data to InvoiceDetailView
- Created src/app/(dashboard)/invoices/[id]/edit/page.tsx — Server Component
  - Queries invoice, client list, and line items
  - Reuses InvoiceForm component with defaultValues pre-populated from invoice data
  - Uses updateInvoice.bind(null, id) for action binding
  - Redirects back to invoice detail page on success via redirectTo prop
  - Back link to invoice detail page, heading "Edit {number}"
- Updated src/actions/invoices.ts:
  - updateInvoice now returns { invoiceId, success: true } to trigger form redirect
- Updated src/components/invoice-form.tsx:
  - Added redirectTo prop (default "/invoices") for configurable post-submit redirect
  - Edit page uses redirectTo={`/invoices/${id}`} to return to detail page
- All checks pass: `bun run check` (tsc + lint + format:check) — 0 errors

Prioritization note:
- Chose 5.4 (invoice detail page) as highest priority because it's the critical-path bottleneck. Without it, clicking any invoice from the list gives a 404. It blocks 5.5 (duplication button), 6.2 (PDF download button), 8.2 (send invoice button), 9.2 (send reminder button), and 11.3 (activity log display). Every downstream feature depends on this page existing.

Next up:
- Feature 5.5: Implement invoice duplication (add duplicateInvoice action + button on detail page)
- Feature 6.1: Install react-pdf and create PDF invoice template
- Feature 6.2: Create PDF generation endpoint

Notes:
- "Send Invoice", "Send Reminder", "Send Overdue Notice", and "Download PDF" buttons are present but disabled — they'll be wired up in Phases 6-9 when the respective features are implemented
- InvoiceForm now supports configurable redirect via redirectTo prop — new page uses default ("/invoices"), edit page redirects back to detail page
- The detail page follows the same Server Component → Client Component pattern as clients/[id]
- No test framework configured yet — verification is via TypeScript compilation and check suite

## 2026-02-19 — Feature 5.5: Implement invoice duplication

Completed:
- Added duplicateInvoice server action to src/actions/invoices.ts:
  - Queries original invoice and its line items by id
  - Generates new invoice number atomically (same transaction pattern as createInvoice)
  - Sets today's date as issuedAt, 30 days from now as dueAt
  - Copies: clientId, currency, notes, internalNotes, reverseCharge, tax settings, line items
  - Does NOT copy: Stripe links, sentAt, paidAt, status (always draft)
  - Logs activity with action "created" and metadata { duplicatedFrom: originalId }
- Added "Duplicate" button to invoice detail page (src/components/invoice-detail.tsx):
  - Uses Copy icon from lucide-react (already imported)
  - Shows "Duplicating..." while in progress
  - On success, redirects to the newly created invoice's detail page
  - Placed between "Download PDF" and "Delete" buttons, visible for all invoice statuses
- All checks pass: `bun run check` (tsc + lint + format:check) — 0 errors
- Phase 5 is now fully complete (5.1–5.5 all pass)

Prioritization note:
- Chose 5.5 (invoice duplication) as highest priority because it completes Phase 5 entirely. It's a small, self-contained feature with no external dependencies, and wrapping up the current phase creates a clean handoff point for Phase 6 (PDF generation).

Next up:
- Feature 6.1: Install react-pdf and create PDF invoice template (critical path — enables PDF download and is needed by 8.2 sendInvoice)
- Feature 6.2: Create PDF generation endpoint (needs 6.1)
- Feature 7.1: Set up Stripe client (needs STRIPE_SECRET_KEY in .env)

Notes:
- The Duplicate button is available for all invoice statuses (draft, sent, paid, overdue, cancelled)
- Duplicated invoices always start as draft with fresh dates and no Stripe/payment data
- The Copy icon is shared with the Stripe payment link indicator — no visual conflict since they appear in different contexts

## 2026-02-19 — Feature 6.1: Install react-pdf and create PDF invoice template

Completed:
- Installed @react-pdf/renderer (v4.3.2) as a dependency
- Created src/components/invoice-pdf.tsx — full PDF invoice template using @react-pdf/renderer:
  - Header: "inv." logo mark (black rounded square + text) with business name and address, invoice number right-aligned with "Invoice" label
  - Bill To section: client name, company, email, address, VAT number
  - Dates section: issued date, due date, currency — right-aligned opposite the client info
  - Line items table: Description, Qty, Rate, Amount columns with zebra-stripe borders
  - Totals section: subtotal, tax (if > 0%), separator line, total — right-aligned
  - Reverse charge notice when applicable (Article 196 EU VAT Directive)
  - Notes section below totals
  - Fixed footer: business VAT number + name on left, bank details on right
- Component accepts typed props: { business, client, invoice } with full type definitions
  - business: name, address, bankDetails, vatNumber (all optional/nullable)
  - client: name, email, company, address, vatNumber
  - invoice: number, currency, issuedAt, dueAt, lineItems[], subtotal, taxRate, taxAmount, total, notes, reverseCharge
- Internal helpers: formatCurrencyPdf (Intl.NumberFormat) and formatDatePdf
- Typography: Helvetica body (10pt), Courier for invoice number and logo, zinc color palette matching app theme
- All checks pass: `bun run check` (tsc + lint + format:check) — 0 errors

Prioritization note:
- Chose 6.1 (react-pdf template) as highest priority because it's the critical-path bottleneck for Phase 6 and beyond. PDF generation (6.2) needs the template, and sendInvoice (8.2) attaches the PDF to emails. Without this, no PDF download or email sending can work. Features 7.x (Stripe) require API keys which may not be configured yet.

Next up:
- Feature 6.2: Create PDF generation endpoint (now unblocked — template exists)
- Feature 7.1: Set up Stripe client (needs STRIPE_SECRET_KEY in .env)
- Feature 10.1: Build settings page (independent, no external dependencies)

Notes:
- The InvoicePdf component is designed for use with both renderToBuffer (server-side for email attachments in 8.2) and API route rendering (6.2)
- Uses StyleSheet.create for type-safe react-pdf styles with `as const` assertions for enum-like values (flexDirection, textAlign, etc.)
- Alphabetical sorting enforced by oxlint perfectionist/sort-objects rule on the styles object
- No test framework configured yet — verification is via TypeScript compilation and check suite
