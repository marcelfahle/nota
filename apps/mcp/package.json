{
  "name": "@nota/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "nota-mcp": "dist/index.js"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "rm -rf dist && tsc -p tsconfig.json && chmod +x dist/index.js",
    "check": "tsc --noEmit",
    "test": "bun test tests"
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^4.3.6"
  }
}
